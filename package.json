{
  "private": true,
  "name": "endif",
  "version": "1.0.0",
  "author": "Aiden Foxx",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently -p none \"npm run build:watch\" \"npm run start\"",
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "test": "jest",
    "test:watch": "jest --watchAll"
  },
  "dependencies": {
    "concurrently": "^7.2.1"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.2",
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "rollup": "^2.75.1",
    "ts-jest": "^28.0.3",
    "typescript": "^4.7.2"
  }
}
